import React from "react";

import classNames from "classnames";

function Footer() {
  return (
    <footer className={classNames("text-slate-400 text-md text-center m-5")}>
      起舞弄清影，何似在人间、
    </footer>
  );
}

export default Footer;
