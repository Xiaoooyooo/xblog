import React from "react";

import classNames from "classnames";

function Footer() {
  return (
    <footer className={classNames("text-xl", "text-center", "m-10")}>
      起舞弄清影，何似在人间、
    </footer>
  );
}

export default Footer;
